name: Plugin Scanner

on:
  pull_request:
  push:
    branches: [main]

jobs:
  plugin-scanner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashgraph-online/ai-plugin-scanner-action@v1
        with:
          path: .
